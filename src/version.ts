/** Installed `chatto-js` package version. */
export const VERSION = "0.1.0";

/** Upstream Chatto release tag used to vendor and generate protocol definitions. */
export const CHATTO_PROTOCOL_TAG = "v0.4.13";
